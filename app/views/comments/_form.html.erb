<%= form_with(url: course_section_comments_path(), scope: "comment") do |form|%>
<% if !parent.nil? %>
<input type="hidden" name=comment[parent_id] value="<%= parent.id %>">
<% end %>

  <%= form.label :content %>
  <%= form.text_area :content %>

  <%= form.submit %>
<% end %>